/**
 * Engine seam: the Stripe payment adapter, server and client halves.
 *
 * See ../index.ts for what this directory is and the rules that govern it.
 */

export { stripeAdapter, stripeAdapterClient } from '@payloadcms/plugin-ecommerce/payments/stripe'
