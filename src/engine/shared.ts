/**
 * Engine seam: shared constants and URL helpers used by the admin views.
 *
 * See ./index.ts for what this directory is and the rules that govern it.
 */

export { PREFERENCE_KEYS, formatAdminURL } from 'payload/shared'
